---
import { Image } from "@astrojs/image/components";
const { data, url } = Astro.props;
const { title, description, date, author, image, tags } = data;
---

<article class="grid gap-1">
  <a href={`/posts/${url}`}>
    <Image
      src={image.src}
      alt={image.alt}
      width={400}
      height={250}
      format="webp"
      fit="cover"
      quality={80}
      aspectRatio="4:1"
      class="w-full"
    />
    <h2 class="text-2xl font-bold">{title}</h2>
  </a>

  <small>by {author} &bull; {date.toDateString()}</small>

  <div class="space-x-2">
    {
      tags.map((tag) => (
        <small class="px-2 py-1 inline-block rounded bg-black">
          <a
            href={`/tags/${tag}`}
            class="before:content-['#'] font-semibold text-white hover:underline"
          >
            {tag}
          </a>
        </small>
      ))
    }
  </div>
</article>
